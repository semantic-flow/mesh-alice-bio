# Alice

This is the governed Markdown source for Alice's main page region.

Alice's integrated biography remains available at [./bio](./bio), and the extracted Bob resource remains available at [../bob](../bob).

This content is carried by the payload artifact `alice/page-main` so the page definition can follow a governed artifact instead of only a direct mesh-local helper file.
